<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Food</title>
    <link rel="stylesheet" href="/Styles/food-view-style.css">    
    <link rel="icon" href="/Icons/foodIcon.png" type="image/png">
</head>
<body>
    <h1>Food menu</h1>
    <div class="add-food">
        <button onclick="location.href='/food/add'">Add Item</button>
    </div>
    <div class="cards">
        <%for(let i=0; i< food.length; i++){%>
            <div class="card">
                <img src="/Imgs/<%= food[i].id %>.png" alt="Food Image">
                <h2><%=food[i].name%></h2>
                <p class="description"><%=food[i].description%></p>
                <p class="price">$<%=food[i].price%></p>
                <%if(food[i].availability == "Yes"){%>
                    <p class="avl">Available</p>
                <%}else{%>
                    <p class="notavl">Not Available</p>
                <%}%>
                <div class="button-group">
                    <button onclick="location.href='/food/<%= food[i].id %>'">Edit Item</button>
                    <button class="delete-btn" onclick="deleteFood('<%= food[i].id %>')">Delete Item</button>
                </div>
            </div>
        <%}%>
    </div>
    <script src="/Scripts/delete.js"></script>
    
</body>
</html>